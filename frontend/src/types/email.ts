export interface EmailTemplate { id: number; name: string; subject: string; body: string; created_by: number; created_at: string; updated_at: string }
export interface RecipientSelection { selection: 'all' | 'selected' | 'filtered'; participant_ids?: number[]; college?: string; attendance?: 'REGISTERED' | 'PRESENT' | 'ABSENT'; resend?: boolean }
export interface RecipientSummary { total_selected: number; invalid_emails: number; recipients_ready: number; invalid_ids: number[] }
export interface CampaignInput extends RecipientSelection { name: string; email_template_id: number | null; sender_name: string; reply_to: string | null; subject: string; body: string; attach_certificate: boolean; send_mode: 'now'; confirmed: boolean }
export interface Delivery { id: number; participant_id: number; participant_name: string; email: string; status: string; attempt_count: number; provider_message_id: string | null; sent_at: string | null; failed_at: string | null; failure_reason: string | null }
export interface Campaign { id: number; name: string; email_template_id: number | null; certificate_template_id: number | null; sender_name: string; reply_to: string | null; subject: string; body: string; attach_certificate: boolean; status: string; created_by: number; created_by_name: string; created_at: string; started_at: string | null; completed_at: string | null; recipient_count: number; sent_count: number; failed_count: number; deliveries?: Delivery[] }
